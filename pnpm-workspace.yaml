packages: [] # pnpm 9 (Vercel's version for lockfile v9) treats any pnpm-workspace.yaml as a monorepo and fails without this
shellEmulator: true

allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true
peerDependencyRules:
  allowedVersions:
    '@vite-pwa/astro>astro': ^6.0.0
