name: CI

on: [push, fork, pull_request, workflow_dispatch, workflow_call]

jobs:

  typos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: crate-ci/typos@master

  lint:
    strategy:
      matrix:
        os: [ubuntu, windows, macos]
      fail-fast: false
    runs-on: ${{ matrix.os }}-latest
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Pull changes
        run: git pull
        continue-on-error: true

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: latest

      - name: Set node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: pnpm

      - name: Install
        run: pnpm install

      - name: Lint
        run: pnpm lint

      - name: Install check tools
        run: pnpm add -D svelte-check @astrojs/check

      - name: Astro check
        run: pnpm astro sync && pnpm exec astro-check

      - name: Svelte check
        run: pnpm exec svelte-check
